# techpmi.github.io

Репозиторий для frotnend-части проекта технологической практики группы ПМИ-11МО

Изменения делаем в ветках, созданных от `main`

После того, как задача будет готова, мержим в `main`

В ветке `gh-pages` лежат файлы для деплоя на techpmi.github.io

Перед первым запуском:

 `npm i`

Для запуска:

`npm run start` 

Собрать в продакшен:

`npm run build` 

